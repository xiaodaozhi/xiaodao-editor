# Keep pnpm's content-addressable store inside the project so all filesystem
# writes stay within the sandbox-allowed project directory.
store-dir=.pnpm-store

# Auto-approve native build scripts for these dependencies (non-interactive).
# esbuild ships its platform binary via an install script.
